/**
 * @deprecated Use `@/src/lib/localUsageDay` for local-midnight resets.
 */
export {
  localUsageDayKeyForClient as utcUsageDayKey,
  msUntilNextLocalReset as msUntilNextUtcReset,
} from '@/src/lib/localUsageDay'
